The Camera folder contains the level camera script which is the main camera controller for the level and controls what the camera sees.

It also contains the camera player tracker which enables zooming on in the player and can handle shaking.