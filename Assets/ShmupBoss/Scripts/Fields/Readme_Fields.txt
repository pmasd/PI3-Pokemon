The Fields folder contains classes responsible for creating game objects with colliders which make a certain action when something enters or exits the fields they represent.

They are all based off the Game Field class which gets its dimensions by the camera view field, background or input.
