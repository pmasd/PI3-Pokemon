The Objects folder contains classes that do not act directly in this pack but are instead part of other classes.

Placing those classes in different files makes it easier to understand the code and to mentally separate different concepts without cluttering the classes they are used in.