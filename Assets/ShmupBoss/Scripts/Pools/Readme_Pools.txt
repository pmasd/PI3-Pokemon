Classes which inherit from Pool Base and function by creating clones of a game object and indexing it in a queue of game objects in a dictionary catagorized by name as key.

Scrolling background, background game objects spawner and even Sfx Source all pool the objects they need but are not listed in this folder since they do not inherit from Pool Base are not somewhat related to this folder.